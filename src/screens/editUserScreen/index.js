import React from "react";
import { NativeBaseProvider, Text } from "native-base";

const EditUserScreen = ({ navigation }) => {
    return (
        <NativeBaseProvider>
            <Text>EDIT userScreen</Text>
        </NativeBaseProvider>
    );
};

export default EditUserScreen;
